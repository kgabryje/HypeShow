import React from "react";
import { View, Text } from "react-native";
import { generalStyles } from "../shared/styles";

export const SimilarShowsScreen = props => (
  <View style={generalStyles.screen}>
    <Text>Similar shows screen</Text>
  </View>
);
